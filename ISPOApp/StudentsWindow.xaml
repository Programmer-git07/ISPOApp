<Window x:Class="IspoApp.StudentsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ИСПО - Студенты" Height="450" Width="850"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="👨‍🎓 Список студентов" FontSize="20" FontWeight="Bold" 
                   Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="btnUpdate" Content="Обновить" Width="100" Height="30" 
                    Margin="0,0,10,0" Click="btnUpdate_Click"/>
            <Button x:Name="btnBack" Content="На главную" Width="100" Height="30" 
                    Click="btnBack_Click"/>
        </StackPanel>

        <DataGrid x:Name="dgStudents" Grid.Row="2" AutoGenerateColumns="True" 
                  IsReadOnly="True" AlternatingRowBackground="#F5F5F5"/>
    </Grid>
</Window>